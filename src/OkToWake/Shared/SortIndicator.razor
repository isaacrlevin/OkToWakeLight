@inject IScheduleFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}
@code {
    /// <summary>
    /// Sets which <see cref=ScheduleFilterColumns"/> this indicator is for.
    /// </summary>
    [Parameter]
    public ScheduleFilterColumns Column { get; set; }

    /// <summary>
    /// The symbol to render.
    /// </summary>
    private string SortSymbol => Filters.SortAscending ? "🔼" : "🔽";
}
